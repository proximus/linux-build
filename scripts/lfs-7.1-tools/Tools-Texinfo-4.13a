PACKAGE='texinfo-4.13a'

configure_commands()
{ :
	./configure --prefix=$TOOLS
}

make_commands()
{ :
	make
}

check_commands()
{ :
	make check
}

install_commands()
{ :
	make install
}

run_command unpack configure make check install clean
